app = "chores-brandonlocke"
primary_region = "ewr"

[env]
  DATABASE_PATH = "/data/chores.db"
  NODE_ENV = "production"
  ORIGIN = "https://chores.brandonlocke.xyz"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  # The volume cannot be shared across machines, so this app runs exactly one.
  min_machines_running = 0

# SESSION_SECRET is set via `fly secrets`, never here.

[[mounts]]
  source = "chores_data"
  destination = "/data"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
